function displayLogin(username) {
  // The 4th line makes objects visible in the for loop.
  // Determines if pre-requisites are met and if so - makes div draggable
  // for new courses 

if (username = "") {

 
      $('#test-button').css("background-color", "white");
    }
}